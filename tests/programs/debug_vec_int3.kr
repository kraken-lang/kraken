// Debug: check len after pushes
fn main() -> int {
    let v: VecInt = vec_int_new();
    vec_int_push(v, 10);
    vec_int_push(v, 20);
    vec_int_push(v, 30);
    let len = vec_int_len(v);
    vec_int_free(v);
    return len;
}
