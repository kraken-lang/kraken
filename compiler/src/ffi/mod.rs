/// Stdlib function signatures: canonical FFI declarations for libc and runtime functions.
pub mod stdlib;
/// FFI type definitions: C ABI types (`CInt`, `SizeT`, `USize`, `ISize`, `VoidPtr`, `FilePtr`).
pub mod types;
